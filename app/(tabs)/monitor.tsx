import { DashboardCommandScreen } from '@/screens/DashboardCommandScreen';

/** Fleet inverter command deck — secondary to ESO Pay (hidden from main tab bar). */
export default DashboardCommandScreen;
